export interface TelegramNodeData {
    label: string 
    credentials: string 
    chatId: string 
    message: string
}

export interface WorkflowData {
    id: string,
    name: string,
    enabled: boolean

}